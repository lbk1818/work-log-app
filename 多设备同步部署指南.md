# 多设备同步部署指南

## 方案概述

使用 Cloudflare Workers + D1 数据库实现多设备同步

```
手机 ←→ Cloudflare Workers ←→ 电脑
           ↓
       D1 数据库（云端存储）
```

---

## 部署步骤

### 第 1 步：注册 Cloudflare 账号

1. 访问 https://dash.cloudflare.com/sign-up
2. 填写邮箱和密码
3. 验证邮箱
4. 完成注册（免费）

### 第 2 步：创建 D1 数据库

1. 登录 Cloudflare 仪表板
2. 左侧菜单 → "Workers & Pages"
3. 点击 "D1 SQL Database" → "Create a database"
4. 数据库名称：`work-logs-db`
5. 点击 "Create"
6. 记录数据库 ID（后面需要）

### 第 3 步：部署 Worker

#### 方法 1：通过仪表板部署（推荐新手）

1. 左侧菜单 → "Workers & Pages" → "Create application"
2. 选择 "Create Worker"
3. Worker 名称：`work-log-sync`
4. 点击 "Deploy"

#### 方法 2：通过 Wrangler CLI 部署（推荐）

```bash
# 安装 Wrangler
npm install -g wrangler

# 登录
wrangler login

# 初始化项目
cd d:\lingma\mobile-work-log
wrangler init

# 配置 wrangler.toml（见下方）

# 部署
wrangler deploy
```

### 第 4 步：配置 Worker

1. 进入 Worker 页面
2. 点击 "Quick edit"
3. 粘贴 `cloudflare-worker.js` 的代码
4. 点击 "Save and Deploy"
5. 记录 Worker URL（例如：`https://work-log-sync.your-subdomain.workers.dev`）

### 第 5 步：绑定 D1 数据库

1. 进入 Worker 页面
2. 点击 "Settings" → "Bindings"
3. 点击 "Add binding"
4. 选择 "D1 Database"
5. 变量名称：`DB`
6. 选择刚才创建的数据库
7. 点击 "Save"

### 第 6 步：配置前端

修改 `cloud-sync.js`：

```javascript
const SYNC_CONFIG = {
    // 替换为您的 Worker URL
    apiEndpoint: 'https://work-log-sync.your-subdomain.workers.dev',
    
    // 所有设备使用相同的 userId
    userId: 'work-log-user-001'
};
```

修改 `index.html`，在 `<head>` 中添加：

```html
<script src="cloud-sync.js"></script>
```

修改 `mobile-app.js`，将 Storage 替换为混合存储：

```javascript
// 混合存储：本地 + 云端
const Storage = {
    localCache: [],
    
    // 初始化：从云端加载
    async init() {
        this.localCache = await CloudStorage.fetchLogs();
        this.saveLocal();
        console.log('从云端同步完成');
    },
    
    getLogs() {
        return this.localCache;
    },
    
    saveLocal() {
        localStorage.setItem('mobile_work_logs', JSON.stringify(this.localCache));
    },
    
    async addLog(log) {
        this.localCache.unshift(log);
        this.saveLocal();
        
        // 异步上传到云端
        CloudStorage.uploadLog(log).then(success => {
            if (success) {
                console.log('云端同步成功');
            }
        });
    },
    
    async updateLog(updatedLog) {
        const index = this.localCache.findIndex(log => log.id === updatedLog.id);
        if (index !== -1) {
            this.localCache[index] = updatedLog;
            this.saveLocal();
            
            // 异步更新云端
            CloudStorage.updateLog(updatedLog);
        }
    },
    
    async deleteLog(id) {
        this.localCache = this.localCache.filter(log => log.id !== id);
        this.saveLocal();
        
        // 异步删除云端
        CloudStorage.deleteLog(id);
    },
    
    // 手动同步按钮调用
    async syncNow() {
        showToast('🔄 正在同步...');
        const success = await CloudStorage.syncAllLogs(this.localCache);
        if (success) {
            showToast('✅ 同步完成');
        } else {
            showToast('❌ 同步失败');
        }
    }
};

// 页面加载时初始化
document.addEventListener('DOMContentLoaded', async function() {
    await Storage.init();
    // ... 其他初始化代码
});
```

---

## 使用方式

### 手机使用
1. 访问应用
2. 自动从云端加载数据
3. 添加/编辑/删除日志自动同步

### 电脑使用
1. 访问相同网址
2. 使用相同的 userId
3. 数据自动同步

### 手动同步
在统计页面添加"同步"按钮：

```html
<button class="btn btn-primary" onclick="Storage.syncNow()">
    🔄 立即同步
</button>
```

---

## 费用说明

Cloudflare 免费额度：
- ✅ 10 万次请求/天（足够个人使用）
- ✅ 5GB 数据库存储
- ✅ 10 万次数据库操作/天
- ✅ 无限带宽

**完全免费！**

---

## 安全建议

1. **使用复杂的 userId**
   ```javascript
   userId: 'user-' + Math.random().toString(36).substr(2, 9)
   ```

2. **或使用密码保护**
   - 添加简单的密码验证
   - 不同设备输入相同密码

3. **定期备份**
   - 导出 Excel 备份到本地
   - 防止意外数据丢失

---

## 故障排查

### Q: 同步失败？
A: 检查：
1. Worker URL 是否正确
2. D1 数据库是否绑定
3. 浏览器控制台是否有错误

### Q: 数据不更新？
A: 清除浏览器缓存，重新加载页面

### Q: 多设备冲突？
A: 使用"立即同步"按钮手动同步

---

## 开始部署

1. 注册 Cloudflare：https://dash.cloudflare.com/sign-up
2. 创建 D1 数据库
3. 部署 Worker
4. 配置前端代码
5. 测试多设备同步

**预计时间：15-20 分钟**

需要我帮您完成哪一步？
